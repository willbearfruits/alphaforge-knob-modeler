import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alphaforge.knobmodeler',
  appName: 'AlphaForge Knob Modeler',
  webDir: 'dist'
};

export default config;
